<div class="devise-center contenedor">
  <h2 class="text-center">Iniciar sesión</h2>
  <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: {class: "form-horizontal", role: "form"}) do |f| %>

    <div class="form-group">
      <label for="emailSess"><b>Email</b></label>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder:"Ingresa tu e-mail", class:"form-control", id:"emailSess", required: true %>
    </div>
    <div class="form-group">
      <label for="pswSess"><b>Contraseña</b></label>
      <%= f.password_field :password, autocomplete: "new-password", placeholder:"Ingresa tu contraseña", class:"form-control", id:"pswSess", required: true %>
    </div>
    <% if devise_mapping.rememberable? -%>
      <div class="field">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me, 'Recordar'%>
      </div>
    <% end -%>
    <%= f.submit "Iniciar sesión", class:"btn btn-primary btn-lg" %>
  <% end %>

  <%= render "devise/shared/links" %>
</div>

<div class="devise-center container">
  <h2 class="text-center">Iniciar sesión</h2>
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <label for="email"><b>Email</b></label>
    <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder:"Ingresa tu e-mail", class:"reg-field", required: true %>

    <label for="psw"><b>Contraseña</b></label>
    <%= f.password_field :password, autocomplete: "new-password", placeholder:"Ingresa tu contraseña", class:"reg-field", required: true %>

    <% if devise_mapping.rememberable? -%>
      <div class="field">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me, 'Recordar'%>
      </div>
    <% end -%>
    <%= f.submit "Iniciar sesión", class:"submit-btn" %>
  <% end %>

  <%= render "devise/shared/links" %>
</div>

<div id="navbar">
  <%= link_to 'FISIOGAD', root_path, id:'logo' %>
  <div id="navbar-right">
    <%= link_to 'INICIO', root_path, class:"active" %>
    <%= link_to 'Galería', galeria_path %>
    <%= link_to 'Contacto', contacto_path %>
    <% if usuario_signed_in? %>
      <%= link_to 'Cerrar Sesión', destroy_usuario_session_path, method: :delete, class:"" %>
      <%= link_to "Hola #{current_usuario.nombre}", edit_usuario_registration_path, class:"", title:"Editar tus datos" %>
    <% else %>
      <%= link_to 'Iniciar Sesión', new_usuario_session_path, class:"" %>
      <%= link_to 'Registrarse', new_usuario_registration_path, class:"" %>
    <% end %>
    <% if usuario_signed_in? && current_usuario.is_admin? %>
      <%= link_to 'Lista de archivos', archivos_path, style:"color:green;" %>
      <%= link_to 'Galería nueva', new_archivo_path, style:"color:green;" %>
    <% end %>
  </div>
</div>



<script>
// When the user scrolls down 80px from the top of the document, resize the navbar's padding and the logo's font size
  window.onscroll = function() {scrollFunction()};

  function scrollFunction() {
    if (document.body.scrollTop > 80 || document.documentElement.scrollTop > 80) {
      document.getElementById("navbar").style.padding = "30px 10px";
      document.getElementById("logo").style.fontSize = "25px";
    } else {
      document.getElementById("navbar").style.padding = "80px 10px";
      document.getElementById("logo").style.fontSize = "35px";
    }
  }
</script>
